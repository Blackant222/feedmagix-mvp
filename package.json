{
  "name": "feed-magix-monorepo",
  "version": "1.0.0",
  "description": "Feed Magix - Landing Page and App Monorepo",
  "scripts": {
    "dev": "concurrently \"npm run dev:app\" \"npm run dev:landing\"",
    "dev:app": "cd app && npm run dev -- --port 3001",
    "dev:landing": "cd landing && npm run dev -- --port 3000",
    "build": "npm run build:app && npm run build:landing",
    "build:app": "cd app && npm run build",
    "build:landing": "cd landing && npm run build",
    "install:all": "npm install && cd app && npm install && cd ../landing && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "app",
    "landing"
  ]
}